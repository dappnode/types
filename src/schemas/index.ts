import manifestSchema from "./manifest.schema.json" assert { type: "json" };
import setupWizardSchema from "./setup-wizard.schema.json" assert { type: "json" };

export { manifestSchema, setupWizardSchema };
